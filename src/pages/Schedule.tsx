import Main from '@/components/Base/Main';
import BaseLayout from '@/components/Layout/BaseLayout';
import ScheduleForm from '@/components/WeekScheduleList';
import React from 'react';

const Schedule = () => {
  return <ScheduleForm />;
};

export default Schedule;
